[buildout]

extends = buildout.cfg

develop = .

[settings]
log-level = INFO
prefix = /opt/birdhouse
user = www-data
etc-user = root

[pywps]
parallelprocesses = 2
mode = slurm
remotehost = wps@slurm
path=/opt/conda/envs/wps/bin

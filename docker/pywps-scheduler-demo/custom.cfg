[buildout]

extends = buildout.cfg

develop = .

[settings]
log-level = INFO
prefix = /opt/birdhouse
user = www-data
etc-user = root

[pywps]
parallelprocesses = 10
maxprocesses = 30
database = sqlite
mode = scheduler
path=/opt/conda/envs/wps/bin
